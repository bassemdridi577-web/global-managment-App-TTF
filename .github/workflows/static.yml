# This workflow is disabled to avoid conflicts with deploy.yml
# The React app deployment is handled by deploy.yml instead

# name: Deploy static content to Pages
# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:
# 
# ... rest commented out
