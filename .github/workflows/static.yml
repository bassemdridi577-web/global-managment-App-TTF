name: Deploy static content to Pages [DISABLED]

on:
  # This workflow is disabled - use deploy.yml instead
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        required: false
        default: 'true'

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: This workflow is disabled
        run: echo "This workflow has been disabled. Use deploy.yml for React app deployment."
