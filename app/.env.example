# Frontend Configuration Example
# Set your API Base URL (if connecting to a live backend)
REACT_APP_API_BASE=http://localhost:5050

# Set to true to run the frontend in mock/portfolio mode without a live backend
REACT_APP_MOCK_API=true

# Disable source maps in production build
GENERATE_SOURCEMAP=false
